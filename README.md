# Countdown Bot

Telegram-бот, который каждый день обновляет закреплённое сообщение в одном
конкретном чате — со списком всех заведённых событий и обратным отсчётом до каждого:

> **Чемпионат Казахстана**
> Осталось 87 дней, Работаем!
>
> **Чемпионат мира**
> Осталось 162 дня, Работаем!

---

## 1. Создать бота в Telegram

1. Напиши [@BotFather](https://t.me/BotFather) → `/newbot` → придумай имя.
2. Скопируй токен (выглядит как `123456:ABC...`).
3. У того же BotFather: `/setprivacy` → выбери своего бота → **Disable**
   (не обязательно, но удобно — пусть видит команды в группах).

## 2. Добавить бота в нужный чат

1. Добавь созданного бота в чат, где должен висеть закреп.
2. Сделай его **администратором** с правом **«Закреплять сообщения»**.
3. В этом чате отправь `/start@твой_бот` — он покажет ID этого чата.

> ℹ️ Команды бота сейчас доступны всем — кто угодно из чата может
> добавлять, удалять и очищать события. Если нужно ограничить доступ
> только админам — скажи, и добавим проверку.

## 3. Локальный запуск (для проверки)

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

export BOT_TOKEN="123456:ABC..."
export TZ="Asia/Almaty"

python bot.py
```

Затем в личке боту:

```
/setup kz 2026-09-01 Чемпионат Казахстана
/setup world 2026-11-15 Чемпионат мира
```

— бот соберёт все события в одно сообщение и закрепит его в чате,
куда его добавили (ближайшие события сверху).

## 4. Деплой на Railway (бесплатный план)

1. Залей этот проект в репозиторий на GitHub.
2. Зайди на [railway.app](https://railway.app) → **New Project** →
   **Deploy from GitHub repo** → выбери репу.
3. В разделе **Variables** добавь:
   - `BOT_TOKEN` = токен от BotFather
   - `TZ` = `Asia/Almaty`
   - (опционально) `UPDATE_HOUR` = `9`, `UPDATE_MINUTE` = `0`
4. Railway сам поймёт `Procfile` и запустит `worker: python bot.py`.

### Альтернатива: Render

1. [render.com](https://render.com) → **New** → **Background Worker**.
2. Подключи репу, build command: `pip install -r requirements.txt`,
   start command: `python bot.py`.
3. Добавь те же переменные окружения.

> ⚠️ **Важно про state.json.** Файл с настройками лежит на диске сервиса.
> На Railway/Render бесплатный план может пересоздавать контейнер —
> при этом `state.json` обнуляется и нужно снова вызвать `/setup`.
> Если хочется надёжности, подключи постоянный диск (Volume на Railway,
> Disk на Render) и поставь `STATE_FILE=/data/state.json`.

## Команды бота

| Команда | Что делает |
|---|---|
| `/start` | Показывает справку и твой `user_id` / ID чата |
| `/setup <slug> YYYY-MM-DD Название` | Добавляет событие или обновляет существующее с тем же `slug`. `slug` — короткий ключ `a-z 0-9 _ -`, до 16 символов |
| `/remove <slug>` | Удаляет событие по ключу |
| `/list` (или `/show`) | Показывает все события и сразу обновляет закреп |
| `/stop` | Очищает все события, обновления прекращаются |

## Как это работает

- При старте и затем **каждый день в 09:00** (по `TZ`) бот:
  1. Считает, сколько дней осталось до каждой целевой даты.
  2. Собирает одно сообщение со всеми событиями, отсортированными по дате (ближайшие сверху).
  3. Открепляет старое (если оно от него), закрепляет новое без уведомления.
- Если все события удалены — закреп просто снимается.
- Состояние (chat_id, список событий, id последнего закрепа) хранится
  в `state.json`. Старый формат с одиночным событием автоматически
  мигрирует в список при первом чтении.
